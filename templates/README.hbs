# **{{ name }}** (version {{ version }})

---

> {{ description }}
{{#if longDescription}}{{ longDescription }}{{/if}}
{{#each engines}}
  ![{{@key}} version](https://img.shields.io/badge/{{@key}}-{{{this}}}-brightgreen.svg)
{{/each}}
